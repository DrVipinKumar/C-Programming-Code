#include<stdio.h>  //header file include:preprocessor directive
int main()
{
    /* code */
  //input   -> process (logic)  -> output   //principle of computer
  int num;
  printf("Hello I am using C commands\n");
  printf("Enter any number"); 
  scanf("%d",&num);
  printf("Your number is=%d",num);
  return 0;
}


//function is block of code, which have name and can be execute again and again, 
//It may be possible to pass arguments to function and it can return value if required
